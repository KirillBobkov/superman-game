export const disablePage = (page) => {
  page.style.display = 'none';
};

export const enablePage = (page) => {
  page.style.display = 'block';
};

export const enableMain = (main) => {
  main.style.display = 'flex';
};